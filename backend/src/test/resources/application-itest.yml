spring:
  datasource:
    url: jdbc:tc:postgresql:latest:///spring_boot_testcontainers?TC_DAEMON=true
    generate-unique-name: true
    hikari:
      minimum-idle: 1
  quartz:
    jdbc:
      initialize-schema: always
  liquibase:
    enabled: true
axon:
  axonserver:
    enabled: false
